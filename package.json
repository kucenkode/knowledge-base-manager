{
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "scripts": {
    "start": "npm install --prefix backend && npm install --prefix frontend && concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\""
  }
}
